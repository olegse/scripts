#!/bin/bash
#
# First find /etc/php/7.2/fpm/pool.d/www.conf and parse
# user and group variable values.
#
# Then set the permissions accordingly, also need to be
# exist global script for parsing any configuration files.
